export {};

let numbers: number[] = [1, 2, 3];

console.log({ numbers });

let nummbers2: Array<number> = [1, 2, 3];

let strings: Array<string> = ["type", "java"];

let sammple: (string | number | boolean)[] = [1, false, "japan", "shimono"];
